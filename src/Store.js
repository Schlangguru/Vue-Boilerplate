import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const state = {
    app_name: 'Vue Boilerplate'
};

const mutations = {

};

const getters = {

};

const store = new Vuex.Store({
    state,
    mutations,
    getters
});

export default store;
